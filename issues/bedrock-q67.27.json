{
  "assignee": "Jason Allum",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-08-20T14:37:03Z",
  "description": "From the 2026-08-20 cold review of the full integration branch (feature/q67-phase-b-prereqs). Mechanical eleganza pass: (1) delete vacuous 'metadata update handling' tests in server_test.exs:901-969 - they send {:metadata_update,...} which no handler ever consumed and assert only Process.alive? via the catch-all; (2) fix doc drift in finalization.ex (metadata_merge_fn/metadata_deferred_fn described at :194/:200/:375 but retired in favor of metadata_apply_fn) and the sharded test name still citing metadata_deferred_fn; (3) drop the unread last_commit_version parameter from metadata_apply_fn (arity 4-\u003e3); (4) ShardRouter.lookup_shard beyond-all-boundaries fallback to the last shard is production-dead behind ingress bounds and was the historical silent-misroute mechanism - raise instead, symmetric with the empty-map raise; (5) one guard one place: delete Metadata.apply_updates' internal version guard, leaving the server pipeline filter as the sole orderer (pending FDB design-audit confirmation that txnStateStore has no internal guard); (6) telemetry in first_rejected_mutation's rescue so validator bugs are visible, not just fail-closed.",
  "id": "bedrock-q67.27",
  "labels": [],
  "parent": "bedrock-q67",
  "comments": [
    {
      "text": "FDB design audit CONFIRMED 'one guard, one place': ApplyMetadataMutation.cpp applies blindly (txnStateStore-\u003eset unconditional at :247-248, keyInfo insert :287, no version comparison anywhere); ordering lives entirely in the pipeline (NotifiedVersion gates; resolver-side exact windows). Bedrock's single filter is the receiver-side pre-filter in server apply_metadata_window (windows deliberately overlap, unlike FDB's exact per-proxy push). Implement: delete the skip branch in Metadata.apply_updates:112-117 (keep per-entry version bookkeeping), move the idempotence claim from metadata.ex moduledoc to the server contract.",
      "timestamp": "2026-08-20T14:47:51Z"
    },
    {
      "text": "Implemented as the first commit of follow-up stack (branch q67.27-review-followups): Metadata.apply_updates internal guard deleted (server pre-filter is the single orderer; FDB txnStateStore applies blindly), router beyond-all-boundaries fallback raises with the key, vacuous {:metadata_update} tests deleted, metadata_merge_fn/metadata_deferred_fn doc drift fixed, unread last_commit_version parameter dropped (apply fn arity 4-\u003e3, later 3-\u003e2 in the converged-verdict commit), trace_ingress_validation_failed telemetry added to the fail-closed rescue. Awaiting stack cold audit.",
      "timestamp": "2026-08-20T15:15:14Z"
    }
  ],
  "priority": 2,
  "status": "in_progress",
  "title": "Review follow-ups: dead words, single version guard, fail-fast router fallback",
  "type": "task",
  "updated_at": "2026-08-20T15:15:14Z"
}
