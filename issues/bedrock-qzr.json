{
  "assignee": "",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-08-15T11:42:49Z",
  "description": "Umbrella for the version-floor design worked out in the q67.20 discussion (2026-08-14/15). Two complementary floors, both expressed purely in sequencer-version arithmetic (versions ARE microsecond timestamps; local clocks appear nowhere):\n\n1. WAL trim floor (write side): Demux owns the bucket math and commands deterministic chunk cuts; ShardServers buffer and obey; confirmed cuts drive the per-log trim min. Consumer-driven, FDB pop model adapted to object storage. Cross-epoch routing stability is a non-goal (replay through Demux + deterministic chunking makes chunks identical across replicas and epochs).\n\n2. MVCC window floor (read side): a read version is an implicit fixed-term lease (term = mvcc_window_us, a single epoch-scoped TSL parameter). No registry, no renewal, no tracking — client-side local deadline for fast-fail, server-side floors as the enforcement authority (olivine window rejection; resolver oldest_version abort). Long-running reads escape to immutable object-storage chunks/snapshots (tracked separately as a GitHub feature issue), so the hot window stays fixed.\n\nDesign decisions banked:\n- All flush/cut/expiry decisions are functions of sequencer versions only.\n- Demux broadcasts version-advance on bucket crossings (heartbeats included) so idle shards flush and never pin the floor.\n- Per-shard tracked state is a \"floor contribution\" (oldest-unflushed - 1, or high-water when drained), not \"last flushed chunk\".\n- Identical-chunk convergence across fr\u003e=2 replicas; put_if_not_exists with :already_exists as confirmation.\n- Replay routes through Demux; recovery pins the floor until the replayed range re-confirms.\n- One W parameter derives olivine window, resolver retention (W + commit-latency margin), chunk grid, and client deadline.",
  "id": "bedrock-qzr",
  "labels": [],
  "priority": 1,
  "status": "open",
  "title": "[EPIC] Version floors: WAL tail trimming and MVCC window enforcement",
  "type": "epic",
  "updated_at": "2026-08-15T11:42:49Z"
}
